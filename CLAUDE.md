# job-monitor

Job monitoring CLI and MCP server. Read `README.md` for usage and `job_monitor/` for implementation. Search, deduplication, storage, notifications, and integrations are separate modules. Use a dry run when checking a pipeline that could send a digest.
